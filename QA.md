# 動作確認

最新確認日：2026年9月17日。Chrome / Playwrightでローカルの完成版を検証。

## 9画像・16ビジュアルガイドの追加（2026年9月17日）

- 新規生成9画像をワークスペースに保存。16ページに5種類のガイドを配置し、ホーム5ページと左メニューに一覧への入口を追加。元の200ルートは維持。
- 16ページを1440 × 1000 / 390 × 844の2サイズで巡回。画像読込失敗、JavaScript実行エラー、ページ全体の横はみ出しは0件。5種類のPC・スマートフォンのスクリーンショットを保存し、代表画面を目視確認。
- 全200ページをPC幅で再巡回し、見出し・本文内リンク・横はみ出し・実行エラーを確認。問題0件。
- ホーム5ページを1440px / 390pxで確認。GAINに4件、他4領域に各3件の画像付き入口を表示。横はみ出しなし。
- ガイド一覧は全16件→Workflow Mapper 3件→注目点1件への絞り込み、ページへの移動、該当0件の案内を確認。モバイルでも開閉・絞り込み・はみ出しを確認。
- 番号選択による説明切替、拡大ダイアログ、Escapeで閉じる操作、操作エリアへのフォーカス移動、3段階の選択、比較の観点切替を確認。
- `npm run check` に画像ファイルの存在、ガイド対象・次の行動のルート、画像説明、注目点位置の検査を追加。既存の計算・スキーマ検査も通過。
- ローカル確認用の手順と画像は `output/playwright/review-visual-guides.cjs`、`review-guide-home.cjs`、`visual-*.png`（Git対象外）。画像の細部は概念表現であり、実設備の仕様・安全設計の検証対象にはしていない。

## 活動説明の反映・5ページ差し替え（2026年9月17日）

- 最新のユーザー説明を全文保存し、5つのホームと5ページを改訂。合計200ページと既存ルートを維持。
- 1440 × 1000で全200ページを再巡回。全見出しの表示、本文中の内部リンクを検査。JavaScript実行エラー、ページ全体の横はみ出し、存在しない内部リンクはいずれも0件。
- GAINホームと差し替えた5ページのスクリーンショットを目視確認。工場の生成画像と番号位置、4階層図、役割区分、QBR、担当・次の行動の表示を確認。
- 変更した10ページ（ホーム5＋差し替え5）を390 × 844でも確認。横はみ出し0件。Robot Projectのモバイル画像を目視確認。
- 新画面の操作確認10項目：4階層切替、役割選択の再読込保存、GAINの障壁と支援リンク、GAINメモ保存、EGCの担当未設定解消、EGC担当保存、QBR確認状態更新、QBR方針保存、生成画像の読込、図の番号から領域の切替。保存はページ再読込後にも保持。検証用入力は確認後に元の値へ復元。
- `npm run check`、`npm run build`が成功。5領域・200ルート、既存の計算・スキーマ・参照先検査を通過。ビルドには500 kBを超えるJSのサイズ注意表示あり（動作エラーではない）。
- ブラウザ確認の手順と画像はローカルの `output/playwright/review-activities.cjs`、`activity-interactions.cjs`、`*-activity.png` に保存（Git対象外）。
- 全200ページのすべての操作組合せを再検証したものではない。実AI・実機・共有バックエンドは引き続き未接続。

## 前回の200ページ版（2026年9月16日・デスクトップのみ）

- 5サイト × 40ページ = 200ページ。各サイトに初版20・第2期10・第3期10ページがあり、ID・タイトルの重複なし。第3期の50ページはすべて別々の操作部品（`npm run check` で重複を禁止）。
- 1440 × 1000で全200ページを巡回（[output/playwright/desktop-200.js](output/playwright/desktop-200.js)）。見出し、本文、`.frontier` の描画を確認。ページ全体の横はみ出し、`main` からの要素はみ出し（装飾の `.art-lines` と横スクロール前提の類似度表を除く）、JavaScript実行エラーは0件。初回の巡回でSIPOCの7列ボードにはみ出しがあり、格子列を `minmax(0, 1fr)` に変更して再確認。
- 第3期の主要操作29項目が成功（[output/playwright/frontier-interactions.js](output/playwright/frontier-interactions.js)）。根拠連鎖の切れ目の移動、文単位の判定と採点、判定木の到達、間隔反復の前進、較正の採点、アンドンの呼び出し・段階到達・再読込後の保持・完了記録、離隔距離の再計算と不足表示、復旧区切りの付け替え、管理手段の警告、料金モデルの最安切替、マイルストーンの保留解除と支払合計、課題文の点検1/5、HHIの10,000、バス係数の訓練予定、経路の順番変更、平準化の緩衝、割り込み集約の効果、リトルの法則の警告、狩野分類の「逆」、統合候補2組、分解ツリーの重なり警告、ホームの2入口（10＋10）。
- 既存機能の修正確認：JSONの書き出しが `{` で始まりBOMを含まないこと、CSVの書き出しは引き続きBOM（EF BB BF）で始まることを、Blobの先頭バイトで確認。
- 自動検査：第3期の20種の計算（狩野分類表6通り、リトルの法則、離隔距離、経路の交差、バス係数、HHI、パーセンタイル、二文字連鎖の類似度、LCSによる適合検査、必要件数、平準化の緩衝、Kingman近似、OEE、Brier、語差分、可搬範囲、段階展開の判定、遅れのコスト）と、判定木の到達先・再生区切り・経路番号・二部グラフの辺・ブループリントの行列数を検証。
- `npm run check` と `npm run build` が成功（JS 567 kB / gzip 182 kB）。
- 統合ブラウザの描画面が固定幅のため、1440px幅の画面全体スクリーンショットは取得できず、レイアウトはDOMの寸法（`innerWidth`・`scrollWidth`・要素の `getBoundingClientRect`）で確認。ユーザー指定により携帯画面の確認は行っていません。

## 前回の150ページ版（デスクトップのみ）

- 5サイト × 30ページ = 150ページ。各サイトに既存20ページと追加10ページがあり、ID・タイトルの重複なし。
- 1440 × 1000で全150ページを巡回。見出し、内容、追加50ページの出典表示を確認。ページ全体の横はみ出しとJavaScript実行エラーは0件。
- 5サイトの代表画面を画像で確認。安定性モニターの目標と平均のラベル重なりを修正し、再確認。
- 主要操作38項目が成功。下書き保護・引き継ぎ・取消・再読込、保存済み状態のJSON出力、比較列の強調、判定根拠の履歴を含む。
- 別途、保存容量超過を模擬し、初回表示時の保存失敗でも警告が表示されることを確認。
- 追加画面では、採点の再集計、定着率の不正値、演習結果、匿名化5項目、充電競合の解消、重大影響の優先表示、根拠未提出時の確認制限、比較の重み変更、ログの不備解消、経路の加重平均、待ち時間の変更、判断ルールの優先順位、参考限界外の検出、効果の重複排除、確認済み効果との区別、実現効果の割合上限を確認。
- 自動検査で10種類の試算例、ゼロ除算・空欄・整数・割合・ファネルの整合性、存在しない日付・うるう日、充電スロットの境界を確認。
- `npm run check` と `npm run build` が成功。
- ユーザー指定により、今回の追加・修正版は携帯画面の確認を行っていません。

## 初版100ページの確認履歴

以下は前回リリース時の記録です。

## ページと表示

- 5サイト × 20ページ = 100ページ。各サイト内でIDとタイトルの重複なし。
- 1440 × 1000と390 × 844の2画面サイズで全100ページを巡回（計200表示）。
- 全ページで目的の見出しを表示。ページ全体の横はみ出し、JavaScript実行エラーなし。
- 広い表と工程図は、それぞれの表示領域内で横スクロール。
- GAINのPC・スマートフォン表示を画像で確認。モバイルの見出し折返しを調整。
- A3の6欄に具体的なサンプル入力があることを確認。

初回確認でSupplier Engagementのホームに分析リンク先の欠落を発見し、技術レーダーに接続する修正後、全ページの巡回を完了。

## 主要操作（19項目）

1. Robotの回収試算：初期値1,200万円、月160時間、3,500円/時間、年間運用費120万円 → 26.1か月。
2. 初期費用600万円へ変更 → 13か月。
3. 年間運用費1,000万円へ変更 → 回収不能として「—」表示。
4. GAINの時間創出：60分→20分、月20回、12人 → 月160時間。
5. EGCの年間換算：21分、月120件、3,500円/時間 → 年176.4万円。
6. 共創実証の予算：1,800万円 − 予備費300万円、1件250万円 → 6件。
7. カンバンで案件を移動し、再読み込み後も保持。
8. 投稿フォームの変更を保存し、再読み込み後も保持。
9. Workflow Mapperから編集済みの改善案をEGCへ引き継ぎ。
10. 根拠未確認の状態で進行判定ボタンが無効。
11. 全項目の確認・理由記入後にデモ判定を保存。
12. プロンプトカードの詳細を表示。
13. ブックマークを追加し、カードの保存状態へ反映。
14. 停止履歴を機体名で絞り込み。
15. 絞り込んだ表をCSVとしてダウンロード。
16. 横断検索から他サイトの暗黙知ページを発見。
17. A3の6つの具体的な入力値を確認。
18. モバイルメニューを開く。
19. モバイルメニューからページ移動後、メニューが閉じる。

## 自動確認

`npm run check` と `npm run build` が成功。GitHub Actionsでも同じ確認とビルドを実行してから公開します。

全150ページの全操作組合せ、実データ接続、複数ユーザー、認証、機体制御、実AI応答は検証対象外です。後者の機能はこのプロトタイプに実装していません。
